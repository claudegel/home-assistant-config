.default_view:
  view: yes
  icon: mdi:home
  entities:
    - group.travels
    - group.forecast_6h

familly_view:
  view: yes
  icon: mdi:account-multiple
  entities:
    - group.presence

surveillance_view:
  view: yes
  icon: mdi:cctv
  entities:
    - group.surveillance

measurement_view:
  view: yes
  icon: mdi:ruler
  entities:
    - group.home_temperatures
    - group.home_climate
    - group.home_humidity
    - group.cigare_cave
    - group.home_power
    - group.washing_machine

multimedia_view:
  view: yes
  icon: mdi:play-network
  entities:
    - group.pvr
    - media_player.emby__kodi__osmc
    - media_player.home_cinema_salon_kodi
    - media_player.chambre_parentale_main
    - media_player.bureau_main
    - group.homecine_network

weather_view:
  view: yes
  icon: mdi:weather-partlycloudy
  entities:
    - group.current
    - group.weather_1h
    - group.forecast_6h
    - group.forecast_24h

network_view:
  view: yes
  icon: mdi:server-network
  entities:
    - group.cabinet
    - group.domot
    - group.pihole
    - group.network
    - group.sensors_battery_alert

nas_view:
  icon: mdi:nas
  view: yes
  entities:
    - group.nas_general
    - group.nas_storage
    - group.nas_volume1
    - group.nas_volume2

system_view: 
  view: yes
  name: System
  entities: 
    - group.ha_system
    - group.all_automations
    - group.all_switches
    - group.all_lights
    - group.all_devices
    - group.all_scripts
